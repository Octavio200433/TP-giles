name: CI/CD Pipeline - Ahorcado

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    # 1. Descargar el código
    - name: Checkout source code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    # 2. Configurar Node.js
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.x'

    # 3. Configurar pnpm
    - name: Setup pnpm
      uses: pnpm/action-setup@v4
      with:
        version: 9

    # 4. Instalar dependencias
    - name: Install dependencies
      run: pnpm install --no-frozen-lockfile

    # 5. Compilar el proyecto
    - name: Compile / Build
      run: pnpm build

    # 6. Unit Tests
    - name: Run Unit Tests & Coverage (Vitest)
      run: pnpm test -- --run

    # 7. Análisis Estático (SonarQube Cloud)
    - name: SonarQube Scan
      uses: SonarSource/sonarcloud-github-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

    # 8. Descargar navegadores para Playwright/Cucumber
    - name: Install Playwright Browsers
      run: pnpm exec playwright install --with-deps chromium

    # 9. Acceptance Tests (Cucumber)
    - name: Run Acceptance Tests (Cucumber)
      run: pnpm at

    # 10. Deploy a GitHub Pages
    - name: Deploy to GitHub Pages
      if: github.ref == 'refs/heads/main' && success()
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        folder: dist
        branch: gh-pages